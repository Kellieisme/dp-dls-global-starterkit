import * as i0 from '@angular/core';
import * as i1 from '@angular/material/button';
import * as i2 from '@angular/material/icon';
import * as i3 from '@jeppesen-foreflight/dp-dls-global-angular/icon-registry';
import { Observable } from 'rxjs';

/** enum for two Mode types */
declare enum Theme {
    /**
     * Light Mode
     */
    LIGHT = "theme-light",
    /**
     * Dark Mode
     */
    DARK = "theme-dark"
}

/**
 * Interface that defines how the theme storage should happen
 */
interface ThemeStorage {
    /**
     * save function for the Theme
     * @todo best to have this as async functions
     * @param theme Theme
     */
    save(theme: Theme): void;
    /**
     * get the Theme value from the storage
     * @todo best to have this as async functions
     */
    get(): Theme;
}

/**
 * Angular service that provides the theme toggle feature.
 * In summary this service adds the `class='theme-light'` to the document.body element and
 * styles change based on the class added to the document.body
 *
 * Also provides a Observable that emits the current theme every time theme changes
 */
declare class ThemeToggleService {
    private document;
    private themeStorage;
    private platformId;
    /**
     * contains the current active theme
     * avoid mutating it directly, instead use `updateCurrentTheme`
     */
    private currentTheme;
    /**
     * BehaviorSubject that detects the theme changes
     */
    private themeChangedSubject;
    /**
     * Observable that emits the current theme when theme changes.
     * Exposed publicly so that other components can use this feature
     */
    themeChanged$: Observable<Theme>;
    constructor(document: Document, themeStorage: ThemeStorage, platformId: object);
    /**
     * Function to mutate the currentTheme
     * @param theme Theme
     */
    private updateCurrentTheme;
    /**
     * Init function that update the application based on the initial theme value
     * Flow as below
     * 1 - If there is a saved theme in the browser - use this as the initial value
     * 2 - If there is no saved theme, Check for the preferred device theme
     * 3 - If device theme is dark, set the init value to `dark`
     * 4 - Else set the default value to `light`
     */
    private init;
    /**
     * Function to get the current theme
     * @returns Current theme
     * Exposed publicly
     */
    getCurrentTheme(): Theme;
    /**
     * Function that toggles the theme
     * Exposed publicly
     */
    toggleTheme(): void;
    static ɵfac: i0.ɵɵFactoryDeclaration<ThemeToggleService, never>;
    static ɵprov: i0.ɵɵInjectableDeclaration<ThemeToggleService>;
}

/**
 * Angular component to switch the Theme.
 * Also developers can create their own components with the use of `ThemeToggleService`
 * @example
 * ```
 * <ba-theme-toggle></ba-theme-toggle>
 * ```
 */
declare class ThemeToggleComponent {
    private themeToggleService;
    constructor(themeToggleService: ThemeToggleService);
    toggle(): void;
    static ɵfac: i0.ɵɵFactoryDeclaration<ThemeToggleComponent, never>;
    static ɵcmp: i0.ɵɵComponentDeclaration<ThemeToggleComponent, "ba-theme-toggle", never, {}, {}, never, never, true, never>;
}

/**
 * Angular module for mode toggling feature
 * Contains
 *  * ThemeToggleComponent
 *  * ThemeToggleService
 */
declare class ThemeToggleModule {
    static ɵfac: i0.ɵɵFactoryDeclaration<ThemeToggleModule, never>;
    static ɵmod: i0.ɵɵNgModuleDeclaration<ThemeToggleModule, never, [typeof i1.MatButtonModule, typeof i2.MatIconModule, typeof i3.IconRegistryModule, typeof ThemeToggleComponent], [typeof ThemeToggleComponent]>;
    static ɵinj: i0.ɵɵInjectorDeclaration<ThemeToggleModule>;
}

export { ThemeToggleComponent, ThemeToggleModule, ThemeToggleService };
